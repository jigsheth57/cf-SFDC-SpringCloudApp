spring:
  application:
    name: authservice
  cloud:
    config:
      uri: http://configserver:8888
      label: tkg-master
      failFast: true
      retry:
        max-attempts: 60
encrypt:
  failOnError: false