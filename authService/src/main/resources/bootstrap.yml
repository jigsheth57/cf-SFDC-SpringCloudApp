spring:
  application:
    name: authservice
  cloud:
    config:
      uri: ${vcap.services.config-server.credentials.uri:http://configserver:8888}
      failFast: true
encrypt:
  failOnError: false

