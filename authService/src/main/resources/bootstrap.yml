spring:
  application:
    name: authservice
  cloud:
    config:
      uri: ${vcap.services.p-config-server.credentials.uri:http://configserver:8888}
      failFast: true
encrypt:
  failOnError: true

