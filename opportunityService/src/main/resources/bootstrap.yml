spring:
  application:
    name: opportunityService
  cloud:
    config:
      uri: ${vcap.services.p-config-server.credentials.uri:http://configserver:8888}
      failFast: true

encrypt:
  failOnError: false
