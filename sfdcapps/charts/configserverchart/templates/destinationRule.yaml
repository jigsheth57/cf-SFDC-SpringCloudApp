apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: configserver
  labels:
    {{- include "sfdcapps.labels" . | indent 4 }}
spec:
  host: configserver
  subsets:
    - name: v2
      labels:
        version: v2
