# Default values for rabbitmqchart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: jigsheth57/configserver
  tag: latest
  pullPolicy: IfNotPresent

configserver:
  port: 8888
  nodePort: 30888
